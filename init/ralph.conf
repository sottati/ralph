# ralph.conf
RALPH_AGENT=opencode
RALPH_MODEL=""
RALPH_CYCLE=""
RALPH_QUALITY_GATE="bun run lint && bun run test:run"
RALPH_COMMIT_PREFIX=TDD
RALPH_MODE=tdd
# Migration generation cmd (e.g. "bunx drizzle-kit generate", "npx prisma migrate dev")
# If empty, migration instructions are omitted from prompts.
RALPH_MIGRATION_CMD=""
